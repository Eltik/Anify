name = "anify-proxy" # Replace with your desired worker name
main = "index.js" # Entry point for your worker
compatibility_date = "2023-10-30" # Use a recent compatibility date

[vars]
# You can define environment variables here if needed
# EXAMPLE_VAR = "example_value"

# Add other configurations as needed, for example:
# [build]
# command = "npm install && npm run build" # If you have a build step

# [env.production]
# name = "anify-proxy-prod"
# Add production-specific settings here if any 

[observability]
enabled = true
head_sampling_rate = 1